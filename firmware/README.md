# smolpad/firmware

The firmware that powers the SmolPad. It currently uses QMK, to reproduce, simply setup QMK environment and copy this folder, rename it to `smolpad` and copy to `qmk_firmware/keyboards/`.

After that, you could compile with `qmk compile -kb smolpad -km default`
